{
  "name": "trading-agent",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ta/web",
    "build:web": "npm run build --workspace @ta/web",
    "test:web": "npm run test --workspace @ta/web"
  }
}

